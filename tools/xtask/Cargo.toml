[package]
name = "xtask"
version = "0.1.0"
edition = "2024"

# not published — internal tooling
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
fs_extra = "1"
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }   # ← add this
thiserror = { workspace = true }
walkdir = "2"
