[package]
name = "application-cli"
version = "0.1.0"
edition = { workspace = true }

[dependencies]
domain      = { path = "../../domain" }
core-config = { path = "../../core/config" }

tracing           = { workspace = true }
